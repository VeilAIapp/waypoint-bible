<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="14dp"
    android:background="@drawable/widget_background">

    <!-- Header row: wordmark + streak -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="8dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="WAYPOINT"
            android:textSize="8sp"
            android:textColor="@color/widget_primary"
            android:textStyle="bold"
            android:letterSpacing="0.15" />

        <TextView
            android:id="@+id/streak_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 day streak"
            android:textSize="10sp"
            android:textColor="@color/widget_text_secondary"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- Verse text -->
    <TextView
        android:id="@+id/verse_text"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="&quot;The steadfast love of the Lord never ceases; his mercies never come to an end.&quot;"
        android:textSize="12sp"
        android:textColor="@color/widget_text_primary"
        android:textStyle="italic"
        android:maxLines="4"
        android:ellipsize="end" />

    <!-- Reference -->
    <TextView
        android:id="@+id/verse_ref"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Lamentations 3:22-23"
        android:textSize="10sp"
        android:textColor="@color/widget_primary"
        android:textStyle="bold"
        android:paddingTop="6dp" />

</LinearLayout>
